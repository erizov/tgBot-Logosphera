# Logosphera Telegram Bot

Мудрый лингвистический бот для изучения английских идиом через философские притчи.

## Описание

Logosphera — это Telegram бот, который помогает пользователям изучать английские идиомы через философский подход. Бот сочетает в себе обучение языку с глубоким пониманием культурного и философского контекста идиом.

## Основные возможности

- 📚 **Изучение идиом по темам**: Выбор темы для изучения
- 🎲 **Случайные идиомы**: Изучение случайных идиом
- 💭 **Философские объяснения**: Глубокое понимание смысла идиом
- 📝 **Практические задания**: Различные типы упражнений
- 💭 **Система рефлексии**: Самоанализ после изучения
- 🏆 **Система достижений**: Мотивация через достижения
- 📊 **Статистика прогресса**: Отслеживание изученных идиом

## Структура проекта

```
tgBot-Logosphera/
├── bot.py                 # Основной файл бота
├── database.py            # Модуль работы с БД
├── modules/               # Модули бота
│   ├── __init__.py
│   ├── greeting.py        # Модуль приветствия
│   ├── topic_selector.py  # Модуль выбора темы
│   ├── idiom_explainer.py # Модуль объяснения идиом
│   ├── practice.py        # Модуль практических заданий
│   └── reflection.py      # Модуль рефлексии
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile            # Образ бота
├── requirements.txt      # Зависимости Python
├── .env.example         # Пример переменных окружения
├── README.md            # Документация
├── QUICKSTART.md        # Быстрый старт
└── COMMANDS.md          # Команды для управления ботом
```

## Установка и запуск

### Требования

- Python 3.11+
- Docker и Docker Compose
- Telegram Bot Token

### Локальная установка

1. Клонируйте репозиторий или скопируйте папку проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
TELEGRAM_TOKEN=your_telegram_bot_token_here
DB_URL=postgres://admin:password@localhost:5432/logosphera
```

4. Запустите PostgreSQL (через Docker):
```bash
docker-compose up -d db
```

5. Запустите бота:
```bash
python bot.py
```

### Запуск через Docker

1. Создайте файл `.env` с вашим токеном:
```bash
TELEGRAM_TOKEN=your_telegram_bot_token_here
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Просмотр логов:
```bash
docker-compose logs -f bot
```

## Структура базы данных

### Таблицы

- **users** — пользователи бота
- **idioms** — идиомы для изучения
- **achievements** — достижения
- **user_progress** — прогресс пользователей
- **user_achievements** — достижения пользователей
- **user_reflections** — ответы рефлексии

## Система достижений

Бот включает 5 уровней достижений:

- 🌟 **Мудрец дня** — изучено 5 идиом
- 💫 **Философский путь** — изучено 10 идиом
- 🔮 **Языковой мастер** — изучено 20 идиом
- 🏰 **Хранитель мудрости** — изучено 50 идиом
- 🐉 **Верховный мудрец** — изучено 100 идиом

## Система рефлексии

После изучения каждой идиомы пользователь может пройти рефлексию:

- **Бинарные вопросы** (да/нет)
- **Краткие открытые вопросы**
- **Развернутые вопросы**

Ответы сохраняются в базе данных для анализа прогресса.

## Разработка

### Добавление новых идиом

Идиомы можно добавлять через SQL или расширить метод `_init_idioms()` в `database.py`.

### Добавление новых тем

Темы добавляются в `TopicSelectorModule.TOPICS` в файле `modules/topic_selector.py`.

### Стиль кода

Проект следует PEP 8:
- Отступ 4 пробела
- Длина строки ≤ 79 символов
- Типы: snake_case для функций/переменных, CapWords для классов
- Type hints для публичных функций

## Команды бота

- `/start` — начать работу с ботом

📖 **Подробные команды:** См. [COMMANDS.md](COMMANDS.md) для полного списка команд Docker, управления БД и отладки.

## Лицензия

Проект создан в образовательных целях.

## Автор

Разработано для изучения английских идиом через философский подход.
