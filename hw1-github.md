# Задание Light: Работа с GitHub

## Цель задания
Научиться работать с Git и GitHub: создать репозиторий, клонировать его, вносить изменения и отправлять их на GitHub.

---

## Шаг 1: Создание репозитория на GitHub

### 1.1. Создание репозитория через веб-интерфейс GitHub

1. Откройте браузер и перейдите на [GitHub.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите на кнопку **"+"** в правом верхнем углу → выберите **"New repository"**
4. Заполните форму:
   - **Repository name**: `tgBot-Logosphera` (или любое другое имя)
   - **Description**: Описание репозитория (опционально)
   - **Visibility**: Public или Private (на ваш выбор)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (если репозиторий уже существует локально)
5. Нажмите **"Create repository"**

### 1.2. Подключение существующего локального репозитория к GitHub

Если у вас уже есть локальный репозиторий в текущей директории:

```bash
# Проверяем текущий статус
git status

# Проверяем, есть ли уже удаленный репозиторий
git remote -v

# Если удаленного репозитория нет, добавляем его
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/tgBot-Logosphera.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/tgBot-Logosphera.git

# Проверяем, что remote добавлен
git remote -v
```

**Скриншот для Шага 1:**
- Скриншот страницы создания репозитория на GitHub
- Скриншот вывода команды `git remote -v` после добавления remote

---

## Шаг 2: Клонирование репозитория

### 2.1. Клонирование в новую директорию (для демонстрации)

```bash
# Переходим в родительскую директорию (или любое другое место)
cd ..

# Клонируем репозиторий
# Замените YOUR_USERNAME на ваш GitHub username
git clone https://github.com/YOUR_USERNAME/tgBot-Logosphera.git tgBot-Logosphera-clone

# Или если используете SSH:
# git clone git@github.com:YOUR_USERNAME/tgBot-Logosphera.git tgBot-Logosphera-clone

# Переходим в клонированную директорию
cd tgBot-Logosphera-clone

# Проверяем содержимое
ls -la

# Проверяем статус
git status
```

**Скриншот для Шага 2:**
- Скриншот вывода команды `git clone`
- Скриншот содержимого клонированной директории (`ls -la`)
- Скриншот вывода `git status`

---

## Шаг 3: Изменение README.md, коммит и пуш

### 3.1. Изменение файла README.md

```bash
# Убедитесь, что вы в клонированной директории
cd tgBot-Logosphera-clone

# Открываем README.md для редактирования
# Можно использовать любой текстовый редактор:
# - nano README.md
# - vim README.md
# - code README.md (если установлен VS Code)
# - notepad README.md (Windows)

# Добавляем или изменяем содержимое README.md
# Например, добавим раздел "Описание проекта"
```

Пример содержимого README.md:
```markdown
# tgBot-Logosphera

Telegram бот для изучения идиом и цитат.

## Описание проекта

Этот проект представляет собой Telegram бот, который помогает пользователям изучать идиомы и цитаты на английском и русском языках.

## Установка

1. Клонируйте репозиторий
2. Установите зависимости: `pip install -r requirements.txt`
3. Настройте переменные окружения в файле `.env`
4. Запустите бота: `python bot.py`

## Использование

Бот поддерживает различные команды для изучения идиом и цитат.
```

### 3.2. Проверка изменений

```bash
# Проверяем статус репозитория
git status

# Просматриваем изменения в файле
git diff README.md
```

**Скриншот для Шага 3.1-3.2:**
- Скриншот измененного содержимого README.md
- Скриншот вывода `git status`
- Скриншот вывода `git diff README.md`

### 3.3. Добавление файла в staging area

```bash
# Добавляем README.md в staging area
git add README.md

# Или добавляем все измененные файлы:
# git add .

# Проверяем статус после добавления
git status
```

**Скриншот для Шага 3.3:**
- Скриншот вывода `git status` после `git add`

### 3.4. Создание коммита

```bash
# Создаем коммит с описанием изменений
git commit -m "Обновлен README.md: добавлено описание проекта и инструкции по установке"

# Проверяем историю коммитов
git log --oneline -5
```

**Скриншот для Шага 3.4:**
- Скриншот вывода `git commit`
- Скриншот вывода `git log --oneline -5`

### 3.5. Отправка изменений на GitHub (push)

```bash
# Отправляем изменения на GitHub
# Если это первый push, используем:
git push -u origin main

# Или если ветка называется master:
# git push -u origin master

# Для последующих push можно использовать просто:
# git push
```

**Важно:** При первом push GitHub может запросить аутентификацию:
- Если используете HTTPS, потребуется Personal Access Token (PAT)
- Если используете SSH, убедитесь, что SSH ключ добавлен в GitHub

**Скриншот для Шага 3.5:**
- Скриншот вывода `git push`
- Скриншот страницы репозитория на GitHub с обновленным README.md

---

## Шаг 4: Проверка на GitHub

1. Откройте браузер и перейдите на страницу вашего репозитория:
   `https://github.com/YOUR_USERNAME/tgBot-Logosphera`

2. Убедитесь, что:
   - README.md обновлен
   - В истории коммитов виден ваш новый коммит
   - Дата последнего коммита соответствует текущей

**Скриншот для Шага 4:**
- Скриншот страницы репозитория на GitHub
- Скриншот истории коммитов
- Скриншот содержимого README.md на GitHub

---

## Дополнительные команды для справки

### Просмотр информации о репозитории

```bash
# Просмотр удаленных репозиториев
git remote -v

# Просмотр информации о ветках
git branch -a

# Просмотр последних коммитов
git log --oneline --graph -10

# Просмотр изменений в последнем коммите
git show HEAD
```

### Работа с ветками

```bash
# Создание новой ветки
git branch feature-branch

# Переключение на ветку
git checkout feature-branch

# Или создание и переключение одной командой:
git checkout -b feature-branch

# Отправка новой ветки на GitHub
git push -u origin feature-branch
```

### Отмена изменений

```bash
# Отмена изменений в файле (до git add)
git checkout -- README.md

# Удаление файла из staging area (после git add, до git commit)
git reset HEAD README.md

# Отмена последнего коммита (сохраняя изменения)
git reset --soft HEAD~1
```

---

## Чек-лист для выполнения задания

- [ ] Репозиторий создан на GitHub
- [ ] Локальный репозиторий подключен к GitHub (или клонирован)
- [ ] README.md изменен
- [ ] Изменения добавлены в staging area (`git add`)
- [ ] Создан коммит (`git commit`)
- [ ] Изменения отправлены на GitHub (`git push`)
- [ ] Изменения видны на GitHub
- [ ] Сделаны скриншоты для каждого шага

---

## Примечания

1. **Аутентификация GitHub:**
   - С 2021 года GitHub не поддерживает пароли для HTTPS
   - Используйте Personal Access Token (PAT) или SSH ключи
   - Инструкция по созданию PAT: [GitHub Docs](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)

2. **Имя ветки:**
   - GitHub по умолчанию использует `main` вместо `master`
   - Если ваша ветка называется `master`, замените `main` на `master` в командах

3. **Конфликты:**
   - Если при push возникает конфликт, сначала выполните `git pull`, разрешите конфликты, затем `git push`

---

## Примеры сообщений коммитов

Хорошие сообщения коммитов:
- `"Добавлено описание проекта в README.md"`
- `"Обновлен README: добавлены инструкции по установке"`
- `"Исправлена опечатка в README.md"`

Плохие сообщения коммитов:
- `"изменения"`
- `"fix"`
- `"update"`

---

## Полезные ссылки

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**Удачи в выполнении задания! 🚀**
