# Команды для управления ботом Logosphera

## Команды Docker

### Запуск и остановка

```bash
# Запустить все сервисы (база данных + бот)
docker-compose up -d

# Запустить только базу данных
docker-compose up -d db

# Остановить все сервисы
docker-compose down

# Остановить и удалить все данные (volumes)
docker-compose down -v

# Перезапустить бота
docker-compose restart bot

# Перезапустить все сервисы
docker-compose restart
```

### Просмотр статуса и логов

```bash
# Проверить статус контейнеров
docker-compose ps

# Просмотр логов бота (в реальном времени)
docker-compose logs -f bot

# Просмотр логов базы данных
docker-compose logs -f db

# Просмотр всех логов
docker-compose logs -f

# Просмотр последних 50 строк логов бота
docker-compose logs --tail=50 bot

# Просмотр логов за последние 10 минут
docker-compose logs --since 10m bot
```

### Управление контейнерами

```bash
# Остановить бота (не удаляя контейнер)
docker-compose stop bot

# Запустить остановленный бот
docker-compose start bot

# Пересобрать образ бота (после изменений в коде)
docker-compose build bot

# Пересобрать и перезапустить
docker-compose up -d --build bot
```

### Работа с базой данных

```bash
# Подключиться к базе данных через psql
docker-compose exec db psql -U admin -d logosphera

# Выполнить SQL команду
docker-compose exec db psql -U admin -d logosphera -c "SELECT * FROM users;"

# Создать резервную копию базы данных
docker-compose exec db pg_dump -U admin logosphera > backup.sql

# Восстановить базу данных из резервной копии
docker-compose exec -T db psql -U admin logosphera < backup.sql
```

## Локальный запуск (без Docker)

### Установка зависимостей

```bash
# Установить все зависимости
pip install -r requirements.txt

# Или с использованием виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### Запуск бота локально

```bash
# Запустить бота (требуется запущенная БД)
python bot.py

# Запустить с выводом в консоль
python bot.py 2>&1 | tee bot.log
```

### Запуск только базы данных через Docker

```bash
# Запустить только PostgreSQL
docker-compose up -d db

# Проверить, что БД запущена
docker-compose ps db
```

## Команды в Telegram боте

После запуска бота, вы можете использовать следующие команды в чате с ботом:

### Основные команды

- `/start` - Начать работу с ботом, показать приветствие и главное меню

### Интерактивные кнопки

Бот использует кнопки для навигации:

- **📚 Выбрать тему** - Выбрать тему для изучения идиом
- **🎲 Случайная идиома** - Получить случайную идиому для изучения
- **🏆 Мои достижения** - Просмотреть разблокированные достижения
- **📊 Статистика** - Просмотреть статистику прогресса

### Процесс изучения

1. Выберите тему или случайную идиому
2. Изучите объяснение идиомы
3. Пройдите практическое задание
4. Пройдите рефлексию (опционально)

## Полезные команды для разработки

### Проверка подключения к базе данных

```bash
# Проверить, что БД доступна
docker-compose exec db pg_isready -U admin

# Подсчитать количество пользователей
docker-compose exec db psql -U admin -d logosphera -c "SELECT COUNT(*) FROM users;"

# Просмотреть все идиомы
docker-compose exec db psql -U admin -d logosphera -c "SELECT expression, topic FROM idioms;"

# Просмотреть прогресс пользователя
docker-compose exec db psql -U admin -d logosphera -c "SELECT u.telegram_id, COUNT(up.idiom_id) as learned FROM users u LEFT JOIN user_progress up ON u.id = up.user_id WHERE up.status = 'completed' GROUP BY u.telegram_id;"
```

### Отладка

**Linux/Mac:**
```bash
# Просмотр логов с фильтрацией ошибок
docker-compose logs bot | grep -i error

# Просмотр логов с фильтрацией по уровню
docker-compose logs bot | grep "ERROR\|WARNING"

# Проверка переменных окружения
docker-compose exec bot env | grep TELEGRAM
```

**Windows PowerShell:**
```powershell
# Просмотр логов с фильтрацией ошибок
docker-compose logs bot | Select-String -Pattern "error" -CaseSensitive:$false

# Просмотр логов с фильтрацией по уровню
docker-compose logs bot | Select-String -Pattern "ERROR|WARNING"

# Проверка переменных окружения
docker-compose exec bot env | Select-String -Pattern "TELEGRAM"
```

### Очистка и перезапуск

```bash
# Полная очистка (удаление всех данных)
docker-compose down -v
docker-compose up -d

# Пересборка образа после изменений
docker-compose build --no-cache bot
docker-compose up -d bot
```

## Примеры использования

### Полный цикл запуска

**Linux/Mac:**
```bash
# 1. Перейти в директорию проекта
cd tgBot-Logosphera

# 2. Проверить наличие .env файла
cat .env

# 3. Запустить все сервисы
docker-compose up -d

# 4. Проверить статус
docker-compose ps

# 5. Просмотреть логи
docker-compose logs -f bot
```

**Windows PowerShell:**
```powershell
# 1. Перейти в директорию проекта
cd E:\Python\GptEngineer\tgBot\tgBot-Logosphera

# 2. Проверить наличие .env файла
Get-Content .env

# 3. Запустить все сервисы
docker-compose up -d

# 4. Проверить статус
docker-compose ps

# 5. Просмотреть логи
docker-compose logs -f bot
```

### Обновление кода

```bash
# 1. Остановить бота
docker-compose stop bot

# 2. Пересобрать образ
docker-compose build bot

# 3. Запустить обновленный бот
docker-compose up -d bot

# 4. Проверить логи
docker-compose logs -f bot
```

### Резервное копирование

```bash
# Создать резервную копию
docker-compose exec db pg_dump -U admin logosphera > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из резервной копии
docker-compose exec -T db psql -U admin logosphera < backup_20260110_120000.sql
```

## Решение проблем

### Бот не отвечает

**Linux/Mac:**
```bash
# Проверить статус
docker-compose ps

# Проверить логи на ошибки
docker-compose logs bot | tail -50

# Перезапустить бота
docker-compose restart bot
```

**Windows PowerShell:**
```powershell
# Проверить статус
docker-compose ps

# Проверить логи на ошибки
docker-compose logs bot --tail=50

# Перезапустить бота
docker-compose restart bot
```

### Проблемы с базой данных

```bash
# Проверить здоровье БД
docker-compose exec db pg_isready -U admin

# Перезапустить БД
docker-compose restart db

# Проверить подключение
docker-compose exec db psql -U admin -d logosphera -c "SELECT 1;"
```

### Проблемы с токеном

**Linux/Mac:**
```bash
# Проверить переменные окружения в контейнере
docker-compose exec bot env | grep TELEGRAM_TOKEN

# Проверить файл .env
cat .env
```

**Windows PowerShell:**
```powershell
# Проверить переменные окружения в контейнере
docker-compose exec bot env | Select-String -Pattern "TELEGRAM_TOKEN"

# Проверить файл .env
Get-Content .env
```
